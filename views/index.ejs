<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Quora Posts</h1>
    <% for(post of posts){ %>
        <div class="post-card">
            <h3>@<%= post.username %></h3>
            <p><%= post.content %></p>
            <div class="button-group">
                <a href="/posts/<%=post.id%>"><button>See in Detail</button></a>
                <a href="/posts/<%=post.id%>/edit"><button>Edit Content</button></a>
                <form method="post" action="/posts/<%=post.id%>?_method=DELETE" style="display: inline;">
                    <button>Delete Post</button>
                </form>
            </div>
        </div>
    <% } %>
    <a href="/posts/new"><button class="create-button">Create New Post</button></a>
</body>
</html>
